# Docker Compose Configuration Template
# This file is used ONLY when running via `docker compose up`
# For standalone development, see backend/.env.example and discord/.env.example

# ==========================================
# Backend Configuration
# ==========================================

# SEGA ID credentials for maimaidx-eng.com login
SEGA_ID=your_sega_id_here
SEGA_PASSWORD=your_sega_password_here

# Backend server port (default: 3000)
BACKEND_PORT=3000

# Database file path (Docker Compose uses /app/data mount)
DATABASE_URL=sqlite:/app/data/maimai.sqlite3

# Fetched data directory path (default: fetched_data)
FETCHED_DATA_PATH=/app/fetched_data

# ==========================================
# Discord Bot Configuration
# ==========================================

# Discord bot token from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Your Discord user ID (Developer Mode → Right-click profile → Copy ID)
DISCORD_USER_ID=your_discord_user_id_here

# Backend API URL (use service name for Docker Compose)
BACKEND_URL=http://backend:3000
