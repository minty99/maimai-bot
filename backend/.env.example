# Backend Standalone Development Configuration
# Copy this file to .env in the same directory for local development

# SEGA ID credentials for maimaidx-eng.com login
SEGA_ID=your_sega_id_here
SEGA_PASSWORD=your_sega_password_here

# Backend server port (default: 3000)
BACKEND_PORT=3000

# Database file path (relative to project root)
DATABASE_URL=sqlite:data/maimai.sqlite3

# Fetched data directory path (default: fetched_data)
FETCHED_DATA_PATH=fetched_data
